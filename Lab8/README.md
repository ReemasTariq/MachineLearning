We applied the **K-Nearest Neighbors (KNN)** algorithm to classify the dataset after performing **feature scaling** and splitting the data into training and testing sets.
We first trained the model with an initial value of K, then evaluated its performance using a confusion matrix and classification report.

After that, we tested multiple values of **K** and analyzed the error rate to find the optimal value.
Finally, we selected the best K (around 31), which improved the model’s accuracy and reduced the overall error.
